# Signal Hub — Credentials Template
#
# SETUP INSTRUCTIONS:
# 1. cp config/credentials.yaml.template config/credentials.yaml
# 2. Fill in your actual credentials
# 3. NEVER commit credentials.yaml to git
#
# Alternatively, set environment variables:
#   ANTHROPIC_API_KEY
#   CLOUDFLARE_API_TOKEN

anthropic:
  api_key: "sk-ant-api03-..."

cloudflare:
  api_token: "your-cloudflare-api-token"
