[Unit]
Description=Signal Hub — daily timer (6 AM ET)
Requires=signal-hub.service

[Timer]
# Run at 6:00 AM Eastern every day.
# Persistent=true catches up if the machine was off at trigger time.
OnCalendar=*-*-* 06:00:00 America/New_York
Persistent=true

[Install]
WantedBy=timers.target
